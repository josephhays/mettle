<%= simple_form_for(@cms_show) do |f| %>
  <% if @cms_show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cms_show.errors.count, "error") %> prohibited this cms_show from being saved:</h2>

      <ul>
      <% @cms_show.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.input :title %>
    <%# f.input :location_id, collection: :get_shows, label_method: :name, value_method: :id, label: "Airtime ID", include_blank: false %>
    <%= f.submit %>
  </div>
<% end %>
